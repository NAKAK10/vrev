{
  "schema_version": 1,
  "queries": [
    {
      "name": "runners.list",
      "permission": "jobs.read",
      "input_schema": { "type": "object", "properties": {}, "additionalProperties": false },
      "output_schema": { "type": "object", "properties": {}, "additionalProperties": false },
      "resources": ["runners"]
    }
  ],
  "commands": [
    {
      "name": "runner.add",
      "permission": "jobs.enqueue",
      "input_schema": { "type": "object", "properties": { "name": { "type": "string", "minLength": 1, "maxLength": 80 }, "command": { "type": "string", "minLength": 1, "maxLength": 2000 } }, "required": ["name", "command"], "additionalProperties": false },
      "output_schema": { "type": "object", "properties": {}, "additionalProperties": false },
      "invalidates": ["runners", "workflow-settings"]
    },
    {
      "name": "runner.test",
      "permission": "jobs.enqueue",
      "input_schema": { "type": "object", "properties": { "runner_id": { "type": "string", "minLength": 1, "maxLength": 128 } }, "required": ["runner_id"], "additionalProperties": false },
      "output_schema": { "type": "object", "properties": {}, "additionalProperties": false },
      "invalidates": ["runners", "workflow-settings"]
    },
    {
      "name": "runner.delete",
      "permission": "jobs.enqueue",
      "input_schema": { "type": "object", "properties": { "runner_id": { "type": "string", "minLength": 1, "maxLength": 128 } }, "required": ["runner_id"], "additionalProperties": false },
      "output_schema": { "type": "object", "properties": {}, "additionalProperties": false },
      "invalidates": ["runners", "workflow-settings"]
    }
  ]
}
